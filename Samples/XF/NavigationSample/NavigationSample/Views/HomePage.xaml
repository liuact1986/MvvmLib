<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:nav="clr-namespace:MvvmLib.Navigation;assembly=MvvmLib.XF"
             nav:ViewModelLocator.ResolveViewModel="True"
             Title="Home Page"
             x:Class="NavigationSample.Views.HomePage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Message}"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Button Text="Go to PageA" Command="{Binding NavigateCommand}"></Button>
            <Button Text="Show Modal Page" Command="{Binding ShowModalPageCommand}"></Button>
            <Button Text="Go to TabbedPage" Command="{Binding NavigateToTabbedPageCommand}"></Button>
            <Button Text="Master Detail" Command="{Binding MasterDetailPageCommand}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>