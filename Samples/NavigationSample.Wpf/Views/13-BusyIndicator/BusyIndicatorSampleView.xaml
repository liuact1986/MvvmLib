<UserControl x:Class="NavigationSample.Wpf.Views.BusyIndicatorSampleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:NavigationSample.Wpf.Views"
             xmlns:controls="clr-namespace:NavigationSample.Wpf.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource PageBackground}">
        <Grid.Effect>
            <DropShadowEffect BlurRadius="5" ShadowDepth="2" Color="{StaticResource ShadowColor}"/>
        </Grid.Effect>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Content="View H (busy)" Command="{Binding Navigation.NavigateCommand}" CommandParameter="{x:Type views:ViewH}"/>
        </StackPanel>

        <ContentControl Content="{Binding Navigation.Current}" Grid.Row="1"/>

        <controls:BusyIndicator x:Name="BusyIndicator" FocusAfterBusy="MessageTextBox" Grid.RowSpan="2"/>
    </Grid>
</UserControl>
