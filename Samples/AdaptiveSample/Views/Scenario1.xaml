<UserControl x:Class="AdaptiveSample.Views.Scenario1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Content="Back" Click="OnGoBack" Height="20" Margin="12"></Button>
            <TextBlock Text="BreakpointListener" Style="{StaticResource HeaderTextBlockStyle}"></TextBlock>
        </StackPanel>
        <TextBlock Text="Allows to define a list of breakpoints and be notified when the current breakpoint changed" Style="{StaticResource CaptionTextBlockStyle}" Margin="12" Grid.Row="1"/>
        <ListView x:Name="List" Grid.Row="2"></ListView>
    </Grid>
</UserControl>
