<Page
    x:Class="ValidationSample.Windows.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ValidationSample.Windows.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontSize="18"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Content="Wrapper Sample" Click="OnWrapperSampleClick" Margin="3"></Button>
            <Button Content="Validatable Sample" Click="OnValidatableAndEditableSampleClick" Margin="3"></Button>
            <Button Content="ViewModel Validatable Sample" Click="OnViewModelValidatableSampleClick" Margin="3"></Button>
        </StackPanel>

        <TextBlock x:Name="Title" Margin="10" FontSize="20" Grid.Row="1"/>

        <ContentControl x:Name="MainContent" Grid.Row="2" HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch">
        </ContentControl>
    </Grid>
</Page>
